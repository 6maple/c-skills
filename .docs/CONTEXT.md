# CONTEXT

Keep this file short.

- Store current task state in `{config.docs.work_items_active_dir}`.
- Store completed/stale task state in `{config.docs.work_items_archive_dir}`.
- Store hard decisions in `{config.docs.adr_dir}`.
