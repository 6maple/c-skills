- 设计应基于可核验的实际实践，并结合真实验收场景进行合理设计，避免缺乏依据、无法验证的凭空设计或过度设计。
- 设计应遵循自上而下、从整体到局部的原则，逐层展开；每次只设计当前层级，并将下一层视为黑盒。
- 每次实现完成后，应从整体视角重新审视设计与实现，避免局部合理但整体产生偏离。
- 通用逻辑需要复用时，应优先提供定义或创建对象的公共辅助函数，由具体模块拥有并传入自身定义；避免通过集中式类型分支和条件工厂统一创建不同领域对象，以保持高内聚、低耦合并减少散弹式修改。
